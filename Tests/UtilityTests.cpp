#include "pch.h"
#include "CppUnitTest.h"
#include <Utility.h>

#include <cassert>

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace Tests
{
	TEST_CLASS(UtilityTests)
	{
	public:
		/*TEST_METHOD(TestRegex)
		{
			Regex regex;
			Assert::IsTrue(regex.Parse("abc[xy]+"));

			Assert::IsTrue(regex.Match("abcxxxyyxyx"));
			Assert::IsFalse(regex.Match("acxxxyyxyx"));
			Assert::IsTrue(regex.Match("abcx"));
			Assert::IsTrue(regex.Match("abcy"));
			Assert::IsFalse(regex.Match("abc"));


			Assert::IsTrue(regex.Parse("[xy]*"));

			Assert::IsTrue(regex.Match("xxxyyxyx"));
			Assert::IsTrue(regex.Match("abc"));
			Assert::IsTrue(regex.Match(""));
			Assert::IsTrue(regex.Match("x"));
			Assert::IsTrue(regex.Match("y"));
		}*/
	};
}
